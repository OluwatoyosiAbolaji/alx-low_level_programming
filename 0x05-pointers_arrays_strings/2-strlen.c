#include <stdio.h>
#include "main.h"

/**
  * _strlen(char *s) - returns the length of string
  * @s: string to be counted

* return: always 0 (success)
*/

int _strlen(char *s)
{
	strlen(*s);
	return (0);
}
